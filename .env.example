VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# For local dev, put VITE_API_BASE_URL=http://localhost:4000 in a separate
# .env.local file (gitignored) instead of here — it must NOT be set in
# production, where the frontend and API are served from the same origin.
